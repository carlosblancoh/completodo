import { Canvas, Meta, Story } from '@storybook/addon-docs';

import {RegisterController } from './RegisterController';

<Meta title="Controller/Register Controller" component={RegisterController} />

# Register Controller

Controlador encargado de manejar la página `Register`.

## Métodos
- `onRegister(name, email, password)`: registra a un usuario.
