import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { NoteController } from './NoteController';

<Meta title="Controller/Note Controller" component={NoteController} />

# Note Controller

Controlador encargado de manejar la vista `Note`.

## Props
- `id`: identificador de la nota que se quiere mostrar.

## Estado
- `data`: datos de la nota.
- `parentData`: datos del grupo padre.

## Métodos
- `onChange(value)`: modifica la nota.
