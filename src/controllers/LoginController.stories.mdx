import { Canvas, Meta, Story } from '@storybook/addon-docs';

import {LoginController } from './LoginController';

<Meta title="Controller/Login Controller" component={LoginController} />

# Login Controller

Controlador encargado de manejar la página `Login`.

## Métodos
- `onLogin(email, password)`: identifica a un usuario.
- `onRecoverPassword(email)`: envía un correo de recuperación de contraseña al usuario.
